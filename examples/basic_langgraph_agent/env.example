# Copy this file to .env and fill in your values.
# .env is gitignored — never commit your actual API key.

OPENAI_API_KEY=your_openai_api_key_here

# Optional: override the default model (gpt-4o-mini)
# OPENAI_MODEL=gpt-4o
